services:
  logguard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - /var/log:/data_to_monitor:ro
      - logguard_data:/app/chat_history
    restart: unless-stopped
    privileged: true
    network_mode: host

volumes:
  logguard_data:
